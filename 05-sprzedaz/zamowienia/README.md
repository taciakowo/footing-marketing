# zamowienia

Miejsce na operacyjną obsługę **zamówień** (poza lokalnym CSV z `02-output-private/`).

## Obecny system

Zamówienia wyciągane z **nazw kontaktów** (Google Contacts) są w `01-system/update_footing_database.py` → `ZAMOWIENIA.csv`.

Ten folder jest na **przyszłą** integrację z WooCommerce (zamówienia ze sklepu).

## Status

Pusty – tylko dokumentacja architektury.
